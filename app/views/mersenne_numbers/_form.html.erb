<%= form_with(model: mersenne_number, local: true) do |form| %>
  <% if mersenne_number.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mersenne_number.errors.count, "error") %> prohibited this mersenne_number from being saved:</h2>

      <ul>
        <% mersenne_number.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :max_number %>
    <%= form.number_field :max_number, required: true %>
  </div>

  <div class="actions">
    <%= form.submit (button_name) %>
  </div>
<% end %>
